# Cloudflare Web Analytics — public beacon token (no leading/trailing spaces).
# Find it at: dash.cloudflare.com → Web Analytics → your site → JS snippet.
# Optional: leave empty to disable analytics in this environment.
PUBLIC_CF_BEACON_TOKEN=
