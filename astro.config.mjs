import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TROCA: https://SEU-USUARIO.github.io
  site: 'https://redeagle48.github.io',
  // TROCA: nome do teu repositório. Se o repo se chamar SEU-USUARIO.github.io, deixa em branco ('/')
  base: '/truechangefail',
});
